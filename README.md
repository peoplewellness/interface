
  # Powell

  This is a code bundle for Powell. The original project is available at https://www.figma.com/design/g1ax0l6PUZb7LAlcr36jpT/Powell.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  